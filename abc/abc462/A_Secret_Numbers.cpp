#include <bits/stdc++.h>
using namespace std;
#define int long long
#define ll long long

string s;

signed main(){
    // ios::sync_with_stdio(false);
    // cin.tie(0);
    
    cin>>s;
    for(char c: s) if(c < 'A') cout<<c;
    cout<<'\n';
    
}
